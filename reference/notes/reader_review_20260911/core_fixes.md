# 2026-09-11 讀者通讀：核心章節補橋與圖例修正

依三讀者通讀後的既定授權實作；只修改10、11、12三份.py，未同步notebook、未執行notebook、未build或push。新增文字沿用speak-human-tw，保留科學條件與短補充分層。

## 完整修改紀錄

### 1. `book/10_point_process.py` 修改前第 239 行

原因：圖前補足Q–Q與KS讀法；保留原有分布檢查不等於完整驗證的限制。

原文：
```text
# 結構與固定率錯設；兩者都看同一份資料。

```

新文：
```text
# 結構與固定率錯設；兩者都看同一份資料。
#
# Q–Q 圖將理論與樣本在相同累積比例下的分位數配成一點，若各點接近對角線，
# 表示兩個分布在這些位置的數值相近。KS 統計量則是兩條累積分布之間最大的
# 垂直距離，數值越大，表示分布差異越明顯。

```

### 2. `book/11_catalog_completeness_b.py` 修改前第 140 行

原因：補足圖註的量尺，區分完整度門檻與檢定p值。

原文：
```text
# 下面把三種方法放在同一張圖上。它們使用同一批事件，因此差異不來自地震活動，而來自判準。圖中的 KS 型方法用模擬校準比較，不應把「沒有拒絕」解讀為證明沒有漏測。大樣本能發現很小的偏離；小樣本則可能根本沒有足夠能力區分模型。
```

新文：
```text
# 下面把三種方法放在同一張圖上。它們使用同一批事件，因此差異不來自地震活動，而來自判準。圖中的 KS 型方法用模擬校準比較，不應把「沒有拒絕」解讀為證明沒有漏測。大樣本能發現很小的偏離；小樣本則可能根本沒有足夠能力區分模型。
#
# 圖上的三個標示都是各方法選出的完整度規模門檻；KS 方法旁的數值也是門檻，不是檢定的 p 值。
```

### 3. `book/11_catalog_completeness_b.py` 修改前第 194 行

原因：mc_c為完整度規模，不是p值；修正標籤，不改數值。

原文：
```text
f"KS 模擬 p 值 = {mc_c:.1f}"
```

新文：
```text
f"KS 模擬法：Mc = {mc_c:.1f}"
```

### 4. `book/11_catalog_completeness_b.py` 修改前第 265 行

原因：移除5.4尚未教的b-positive參考線所用變數；全檔核對只有該圖使用d_pos與b_pos。

原文：
```text
d_pos = np.diff(obs)[np.diff(obs) > 0]
b_pos = {dc: np.log1p(DM / (d_pos[d_pos >= dc - 1e-9].mean() - dc))
             / (L10 * DM) for dc in (0.2, 0.6)}

```

新文：
```text
（移除）
```

### 5. `book/11_catalog_completeness_b.py` 修改前第 276 行

原因：5.4只保留連續、離散估計與真值；後續5.7已有完整規模差比較，不重複移圖。

原文：
```text
for dc, color in [(0.2, PALETTE[4]), (0.6, PALETTE[2])]:
    fig.add_hline(y=b_pos[dc], line_color=color, line_dash="dot",
                  annotation_text=f"b-positive（trim {dc}）＝ {b_pos[dc]:.3f}")

```

新文：
```text
（移除）
```

### 6. `book/11_catalog_completeness_b.py` 修改前第 371 行

原因：把隱藏程式中的A/B配對規則帶到圖前，明確說明未實作的方法。

原文：
```text
# 下面用時變偵測能力的合成序列比較三種取差方式。橫軸是保留差值的門檻，並不是完整度規模 $M_c$。先看曲線在門檻增加時是否接近真值，再留意高門檻端的波動。Tinti 與 Gasperini（2024）的比較也以這種方式區分估計公式、配對規則與不完整情境。
```

新文：
```text
# 下面用時變偵測能力的合成序列比較三種取差方式。橫軸是保留差值的門檻，並不是完整度規模 $M_c$。先看曲線在門檻增加時是否接近真值，再留意高門檻端的波動。Tinti 與 Gasperini（2024）的比較也以這種方式區分估計公式、配對規則與不完整情境。
#
# 圖中正差和負差都取自時間順序上相鄰的事件，負差以其大小表示。不重疊配對則將第 1、2 個事件配成一對，第 3、4 個配成下一對，再取各對的絕對差，每個事件只用一次。本圖沒有實作前面提到的「尋找下一個足夠大事件」配對法。
```

### 7. `book/11_catalog_completeness_b.py` 修改前第 403 行

原因：以實際配對方式命名圖例，讀者不需回查A/B代碼。

原文：
```text
"正差（取法 A）"
```

新文：
```text
"相鄰正差"
```

### 8. `book/11_catalog_completeness_b.py` 修改前第 404 行

原因：以實際配對方式命名圖例，讀者不需回查A/B代碼。

原文：
```text
"負差（取法 A）"
```

新文：
```text
"相鄰負差的大小"
```

### 9. `book/11_catalog_completeness_b.py` 修改前第 405 行

原因：以實際配對方式命名圖例，讀者不需回查A/B代碼。

原文：
```text
"絕對差（取法 B）"
```

新文：
```text
"不重疊配對絕對差"
```

### 10. `book/12_clustering_laws.py` 修改前第 264 行

原因：圖前分清兩個抽樣問題，防止把排序差期望誤讀成固定主震規模差的下界。

原文：
```text
# 下面左圖由目錄以指定視窗規則選出序列，右圖則是上述理想獨立抽樣的計算。兩者回答不同問題：左圖讓我們看見序列定義的影響，右圖隔離了抽樣極值本身的變化。
```

新文：
```text
# 下面左圖由目錄以指定視窗規則選出序列，右圖則是上述理想獨立抽樣的計算。兩者回答不同問題：左圖讓我們看見序列定義的影響，右圖隔離了抽樣極值本身的變化。
#
# 右圖的排序差參考線，表示一組獨立 GR 樣本中最大與次大規模的間距期望。另一條曲線則固定主震規模，再減去抽樣得到的最大餘震規模；兩者是不同的抽樣問題，不能把排序差參考線當成這條曲線的下界。
```

### 11. `book/12_clustering_laws.py` 修改前第 325 行

原因：修正與正文衝突的下界稱呼；參考線數值不變。

原文：
```text
f"Utsu 下界 1/β = {1/BETA:.2f}"
```

新文：
```text
f"排序差期望 1/β = {1/BETA:.2f}"
```

### 12. `book/12_clustering_laws.py` 修改前第 332 行

原因：圖題直接交代比較量，避免兩張臉的戲劇性表述。

原文：
```text
f"Båth 定律的兩張臉：實測平均 {bath.mean():.2f}"
```

新文：
```text
f"最大餘震規模差：實測平均 {bath.mean():.2f}"
```

## 驗證與重跑需求

- 三份.py的AST解析通過。既有正文行內／顯示公式、連結及標籤全部逐字保留；各章延伸閱讀全文不變。
- 三章code cell數不變，以下索引為0-based且包含setup cell；修改圖例／圖題仍需重跑對應cell更新輸出。

- `10_point_process.py`：6個code cells，改動索引[]。

- `11_catalog_completeness_b.py`：7個code cells，改動索引[2, 4, 6]。

- `12_clustering_laws.py`：7個code cells，改動索引[4]。

- 10章僅補正文，無code變更；同步markdown即可。
- 11章須更新三種Mc圖（code 2）、連續/離散比較圖（code 4）、規模差圖（code 6）。數值估計/亂數seed/其餘資料流程不變；只移除早期b-positive線及其專用計算。
- 12章須更新最大餘震規模差圖（code 4），只有名稱和圖題變更。
- 全檔搜尋確認d_pos與b_pos僅供被移除的早期參考線使用，刪後無殘留引用；後續b_trim的dc是函式參數，後續color由自己的for迴圈賦值。未執行圖表；主代理仍需重跑／同步及建置驗收。

## 補讀stream

已完整讀取`book/_build/reader_pack/STREAM_OUTPUTS.json`四頁輸出。未將原閱讀包缺少stream誤判為網站沒顯示數值。03 HWA水位有效點2242、水溫8928與正文缺測措辭，以及04 TTN殘差nan與參考站比較，已交主代理轉觀測代理核對；未在本子任務修改其檔案。
