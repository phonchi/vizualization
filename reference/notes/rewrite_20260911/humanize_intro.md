# speak-human-tw：首頁與基礎篇

逐句校訂共 21 處。以下原句與改寫含相鄰行，便於定位；行號依修改前快照。

## 1. book/00_intro.md：原第 37 行

**原句**

```text
看到異常與證明可預報之間還有哪些步驟，會在研究方法與臺灣展望中接著討論。
這些物理觀測與統計模型並非兩個分開的主題：它們都需要回答資料支持什麼，
以及還有哪些其他解釋。
```

**為什麼要改**：改成直接的教學敘述，減少多餘對比與總結，保留原來的區分和前後銜接。

**改成**

```text
看到異常與證明可預報之間還有哪些步驟，會在研究方法與臺灣展望中接著討論。
閱讀物理觀測時，仍要使用前面學過的統計判斷：資料支持什麼，
以及還有哪些其他解釋。
```

## 2. book/00_intro.md：原第 45 行

**原句**

```text
主文保留問題的動機、圖表解讀、必要公式與重要限制。短補充可選擇展開，
長推導與演演算法集中在第三部；不展開它們，也能沿著教學主線讀下去。
需要追查某一步時，正文會連到對應附錄，附錄也提供回程連結。
```

**為什麼要改**：修正字詞轉換留下的重複字，恢復「演算法」。

**改成**

```text
主文保留問題的動機、圖表解讀、必要公式與重要限制。短補充可選擇展開，
長推導與演算法集中在第三部；不展開它們，也能沿著教學主線讀下去。
需要追查某一步時，正文會連到對應附錄，附錄也提供回程連結。
```

## 3. book/09_forecasting_intro.py：原第 35 行

**原句**

```text
# 目前沒有能穩定達成這種要求的方法。統計預報改以機率描述未來活動，仍然需要
# 具體的區域、期間與目標；使用機率不是降低檢查標準，而是讓不確定性成為答案的一部分。
#
```

**為什麼要改**：改成直接的教學敘述，減少多餘對比與總結，保留原來的區分和前後銜接。

**改成**

```text
# 目前沒有能穩定達成這種要求的方法。統計預報改以機率描述未來活動，仍然需要
# 具體的區域、期間與目標，並把不確定性明確寫進答案；檢查標準並不因此降低。
#
```

## 4. book/10_point_process.py：原第 139 行

**原句**

```text
#
# 這個概似形式建立在具條件強度、區域性可積且不爆發的簡單點過程等常見條件上。
# 估計參數的方法稍後再學；這裡先理解，模型必須同時對有事件與沒事件的地方
```

**為什麼要改**：按數學或曲線語境表達局部性，避免誤讀成地理區域。

**改成**

```text
#
# 這個概似形式建立在具條件強度、局部可積且不爆發的簡單點過程等常見條件上。
# 估計參數的方法稍後再學；這裡先理解，模型必須同時對有事件與沒事件的地方
```

## 5. book/10_point_process.py：原第 312 行

**原句**

```text
# 上排橫軸是事件序號 $j$，縱軸是累積條件率 $\tau_j$。在一段活動比模型
# 預期密集的期間，很多新事件對應很少的模型時間，區域性曲線會偏平；相反的
# 安靜期則可能形成較陡的增量。不能只看曲線位於對角線哪一側，就把此前累積
```

**為什麼要改**：按數學或曲線語境表達局部性，避免誤讀成地理區域。

**改成**

```text
# 上排橫軸是事件序號 $j$，縱軸是累積條件率 $\tau_j$。在一段活動比模型
# 預期密集的期間，很多新事件對應很少的模型時間，這一段曲線會偏平；相反的
# 安靜期則可能形成較陡的增量。不能只看曲線位於對角線哪一側，就把此前累積
```

## 6. book/10_point_process.py：原第 327 行

**原句**

```text
#
# 我們已經用過模擬來理解模型。現在把生成的方向講清楚：給定參數與起始歷史，
# 依模型產生下一次事件，更新歷史，再繼續往前。重複許多次，就能看見相同
```

**為什麼要改**：刪去重複的學習成果宣告或過場語，直接承接具體觀察。

**改成**

```text
#
# 前面的圖用模擬比較模型。生成一份目錄時，先給定參數與起始歷史，
# 依模型產生下一次事件，更新歷史，再繼續往前。重複許多次，就能看見相同
```

## 7. book/10_point_process.py：原第 405 行

**原句**

```text
# 模型。不能拿空歷史起步的一份目錄，與包含長期既往活動的一份目錄直接比較，
# 再把差異全歸因於演演算法。
#
```

**為什麼要改**：修正字詞轉換留下的重複字，恢復「演算法」。

**改成**

```text
# 模型。不能拿空歷史起步的一份目錄，與包含長期既往活動的一份目錄直接比較，
# 再把差異全歸因於演算法。
#
```

## 8. book/10_point_process.py：原第 457 行

**原句**

```text
# 兩種模擬得到的分布可用來核對實作是否相容；有限次模擬的接近仍不是等價性
# 的證明。分支表示的理論條件與演演算法的數值近似，也需要另外確認。
#
```

**為什麼要改**：修正字詞轉換留下的重複字，恢復「演算法」。

**改成**

```text
# 兩種模擬得到的分布可用來核對實作是否相容；有限次模擬的接近仍不是等價性
# 的證明。分支表示的理論條件與演算法的數值近似，也需要另外確認。
#
```

## 9. book/10_point_process.py：原第 465 行

**原句**

```text
#
# 到這裡，條件強度已經把幾件原本分開的事接起來：它描述歷史如何改變率，
# 概似讓我們比較參數，時間變換幫助檢查遺漏的結構，模擬則提供可能的未來。
```

**為什麼要改**：刪去重複的學習成果宣告或過場語，直接承接具體觀察。

**改成**

```text
#
# 條件強度描述歷史如何改變率；沿著這個定義，
# 概似讓我們比較參數，時間變換幫助檢查遺漏的結構，模擬則提供可能的未來。
```

## 10. book/foundation_catalog.py：原第 24 行

**原句**

```text
# 這些欄位看起來很熟悉，卻還沒有告訴我們什麼是一次「增加」、一群「餘震」，
# 或一段值得注意的「安靜」。這些判斷都需要比較的物件與觀察的尺度。
#
```

**為什麼要改**：這裡指比較對象，不是程式物件。

**改成**

```text
# 這些欄位看起來很熟悉，卻還沒有告訴我們什麼是一次「增加」、一群「餘震」，
# 或一段值得注意的「安靜」。這些判斷都需要比較的對象與觀察的尺度。
#
```

## 11. book/foundation_catalog.py：原第 61 行

**原句**

```text
# 接連發生」，就看相鄰事件的間隔；若要問「較大的事件集中在哪裡」，則要同時
# 看位置與規模。這些不是互相競爭的圖，而是把同一個問題的不同部分拆開。
#
```

**為什麼要改**：改成直接的教學敘述，減少多餘對比與總結，保留原來的區分和前後銜接。

**改成**

```text
# 接連發生」，就看相鄰事件的間隔；若要問「較大的事件集中在哪裡」，則要同時
# 看位置與規模。這幾種圖各自保留不同資訊，可以一起閱讀。
#
```

## 12. book/foundation_catalog.py：原第 128 行

**原句**

```text
# 只看到下方曲線的人，可能把斜率下降解讀成地下變安靜了。知道偵測流程的人，
# 則會先檢查資料是否完整。這也說明完整度不能只當成清理資料的小步驟：
# 它決定了我們對活動變化的解釋。{doc}`11_catalog_completeness_b`會進一步
# 說明如何從規模分布判斷可用的門檻。
```

**為什麼要改**：改成直接的教學敘述，減少多餘對比與總結，保留原來的區分和前後銜接。

**改成**

```text
# 只看到下方曲線的人，可能把斜率下降解讀成地下變安靜了。知道偵測流程的人，
# 則會先檢查資料是否完整。完整度會直接影響我們如何解釋活動變化，
# 需要和發生率一起考慮。{doc}`11_catalog_completeness_b`會進一步
# 說明如何從規模分布判斷可用的門檻。
```

## 13. book/foundation_catalog.py：原第 149 行

**原句**

```text
#
# 我們已經看見合成目錄有些時段擠、有些時段疏。但「擠在一起」究竟超過隨機
# 波動多少？只要是隨機資料，不就也會偶爾連續發生嗎？
```

**為什麼要改**：刪去重複的學習成果宣告或過場語，直接承接具體觀察。

**改成**

```text
#
# 合成目錄有些時段擠、有些時段疏。但「擠在一起」究竟超過隨機
# 波動多少？只要是隨機資料，不就也會偶爾連續發生嗎？
```

## 14. book/foundation_randomness.py：原第 48 行

**原句**

```text
# %% [markdown]
# 中間的隨機撒點也有短暫的密集和空白。這正是比較基準的用途：看到幾個點靠近，
# 還不足以排除隨機性。下方分群序列則在更多地方同時出現短間隔與較長空白，
```

**為什麼要改**：刪去重複的學習成果宣告或過場語，直接承接具體觀察。

**改成**

```text
# %% [markdown]
# 中間的隨機撒點也有短暫的密集和空白。因此，看到幾個點靠近，
# 還不足以排除隨機性。下方分群序列則在更多地方同時出現短間隔與較長空白，
```

## 15. book/foundation_randomness.py：原第 140 行

**原句**

```text
# 上次事件的時間；**自我修正模型**則讓事件發生後的率下降，表達某種累積與
# 釋放的想法。它們不是一份由差到好的排行榜，而是對歷史資訊如何影響未來的
# 不同假設。同一個地區在短期餘震與長期斷層複發的問題中，也可能需要不同描述。
#
```

**為什麼要改**：改成直接的教學敘述，減少多餘對比與總結，保留原來的區分和前後銜接。

**改成**

```text
# 上次事件的時間；**自我修正模型**則讓事件發生後的率下降，表達某種累積與
# 釋放的想法。這些模型對歷史資訊如何影響未來，各自作了不同假設。同一個地區在短期餘震與長期斷層複發的問題中，也可能需要不同描述。
#
```

## 16. book/foundation_randomness.py：原第 155 行

**原句**

```text
#
# 現在我們已能說出模型之間真正的差異：它們如何利用已經發生的事，而不只是
# 能不能畫出密集的一團點。若新來一個事件，固定率模型不改變預報；自激發模型
# 會提高近期活動；更新模型則重新計算距上次事件的時間。
```

**為什麼要改**：刪去重複的學習成果宣告或過場語，直接承接具體觀察。

**改成**

```text
#
# 比較模型時，要看它們如何利用已經發生的事，不能只看是否畫得出密集的一團點。若新來一個事件，固定率模型不改變預報；自激發模型
# 會提高近期活動；更新模型則重新計算距上次事件的時間。
```

## 17. book/foundation_inference.py：原第 75 行

**原句**

```text
#
# 因此熟悉的「次數除以時間」不只是直覺演算法，也是這個模型的最大概似估計。
# 但概似不是參數的機率密度：橫軸不同的 $\mu$ 是我們拿來比較的設定，
```

**為什麼要改**：改成直接的教學敘述，減少多餘對比與總結，保留原來的區分和前後銜接。

**改成**

```text
#
# 因此，熟悉的「次數除以時間」正是這個模型的最大概似估計。
# 但概似不是參數的機率密度：橫軸不同的 $\mu$ 是我們拿來比較的設定，
```

## 18. book/foundation_inference.py：原第 173 行

**原句**

```text
#
# 到這裡，我們已經把三個層次分開：事件怎麼產生、資料如何限制參數，以及
# 參數未知如何影響未來預報。接下來在{doc}`13_etas_structure`，將背景、
```

**為什麼要改**：刪去重複的學習成果宣告或過場語，直接承接具體觀察。

**改成**

```text
#
# 接下來閱讀模型時，要分清事件怎麼產生、資料如何限制參數，以及
# 參數未知如何影響未來預報。接下來在{doc}`13_etas_structure`，將背景、
```

## 19. book/appendix_a_point_process.md：原第 15 行

**原句**

```text

假設所需的絕對連續性與區域性可積性成立，取極限得到
$S'_0(t)=-\lambda_0(t)S_0(t)$。以 $S_0(t_0)=1$ 積分：
```

**為什麼要改**：按數學或曲線語境表達局部性，避免誤讀成地理區域。

**改成**

```text

假設所需的絕對連續性與局部可積性成立，取極限得到
$S'_0(t)=-\lambda_0(t)S_0(t)$。以 $S_0(t_0)=1$ 積分：
```

## 20. book/appendix_a_point_process.md：原第 47 行

**原句**

```text
取對數即得到正文的{eq}`eq:pp-loglik`。這是相對於事件時間的密度形式，
不是某一串精確實數時間發生的正機率。它適用於具有相應條件密度、強度區域性
可積且不爆發的常見簡單點過程；不能不加條件地推廣到所有隨機點集合。
```

**為什麼要改**：按數學或曲線語境表達局部性，避免誤讀成地理區域。

**改成**

```text
取對數即得到正文的{eq}`eq:pp-loglik`。這是相對於事件時間的密度形式，
不是某一串精確實數時間發生的正機率。它適用於具有相應條件密度、強度局部
可積且不爆發的常見簡單點過程；不能不加條件地推廣到所有隨機點集合。
```

## 21. book/appendix_a_point_process.md：原第 154 行

**原句**

```text
正文只取內部完整間隔，但其邊際分布仍是上式，指數不會因取了 $N-1$ 個間隔
而改成 $N-1$。这些間隔也不是互相獨立的。

```

**為什麼要改**：修正殘留簡體字。

**改成**

```text
正文只取內部完整間隔，但其邊際分布仍是上式，指數不會因取了 $N-1$ 個間隔
而改成 $N-1$。這些間隔也不是互相獨立的。

```
